FROM openjdk:latest

# install needed packages
RUN apt-get update && apt-get install -y ant=1.9.9-1+deb9u1  && apt-get install -y zip && apt-get install -y xmlstarlet
RUN mkdir -p izpack

COPY /ant-lib /usr/share/ant/lib/
COPY . izpack
WORKDIR /izpack
RUN ant -file src/build.xml all
  